# Pinned dependencies for reproducible installs.
# Generated from a clean Python 3.12 environment on 2026-07-28 via `pip freeze`.
# Audited with `pip-audit`: no known vulnerabilities as of 2026-08-04.
#
# Pruned on 2026-08-08 when the terminal GUI was removed. That dropped
# `textual` and `pyperclip` and, with them, nine transitive packages
# (rich, Pygments, markdown-it-py, mdit-py-plugins, mdurl, linkify-it-py,
# uc-micro-py, platformdirs, typing_extensions). A cryptographic tool has no
# business pulling in a Markdown parser and a syntax highlighter, and every
# package here is attack surface someone has to trust.
#
# Update carefully — this is a cryptographic tool. After any change, re-run:
#   pip-audit
#   python -m pytest tests/
#
# To install: pip install -r requirements-lock.txt
# To refresh: install requirements.txt into a clean venv, then `pip freeze`
#
# Must stay consistent with the floors declared in pyproject.toml. The previous
# revision pinned cryptography==41.0.7, which both carried known advisories and
# violated the project's own `cryptography>=44.0.0` requirement.
#
# cryptography 49.0.0 -> 50.0.0 on 2026-08-04 for PYSEC-2026-3552 /
# CVE-2026-69247: PKCS#7 EnvelopedData decryption leaked a Bleichenbacher
# oracle through distinguishable errors and timing. MORPHEUS is not affected —
# it uses no RSA, no PKCS#7 and no PKCS#1 v1.5 anywhere — but a cryptographic
# tool should not ship a lock file carrying a known advisory, and `pip-audit`
# in CI gates on exactly that.

# --- Direct dependencies ---
argon2-cffi==25.1.0
cryptography==50.0.0

# --- Optional: post-quantum support (the [pq] extra) ---
# Required only for --hybrid-pq. Remove this line if you do not need it.
pqcrypto==0.4.0

# --- Transitive dependencies ---
argon2-cffi-bindings==25.1.0
cffi==2.1.1
pycparser==3.0
